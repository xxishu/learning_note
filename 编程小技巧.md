- 如果循环中出现 i - 1 等操作，需要处理 i - 1 的边界问题。
- for循环与while循环中，for循环 i++ 永远在循环结束的时候出现，所以若循环中出现 i 对答案进行更新的时候，答案永远不会出错，while会在即将跳出循环时出现错误。

- 双指针算法中，指针在**循环**中移动时一定要与边界判定条件配对

- 加速cin速度

  ```c++
  ios::sync_with_stdio(0);
  cin.tie(0);
  ```

- 前缀和计算中，若在原数组上计算，可以省下一组数组

