

```c++
template<Typename T>
int find(const vector<T> &t, const int x)const{
    int i = 0, j = t.size() - 1;
    while(i < j){
        int mid = i + j + 1 >> 1;//3. 根据更新条件为l = mid，为了避免00划分，mid取l + r + 1 >> 1
        if(q[mid] <= x) l = mid;//1. 根据x >= q[mid]了解到 x在q[mid]的右边，所以左边的值可以舍去 2. 根据q[mid] <= x 得到划分区间为<=x和>x
        else r = mid - 1;
    }
    //若数组中不存在x，则返回的值由if语句中的判断条件为准，若判断条件为<=x，则返回值l也在<=x的区间内
    return l;
    
}
```

