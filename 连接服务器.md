服务器地址 10.2.134.6

公共账号 geek 密码 geek

公网地址 47.96.141.117 ssh服务端口号 6000



以下操作需要创建好账号

服务器端：

首次启动vnc需要输入密码

```
vncserver
```

将./.vnc/xstartup文件修改为

```
cat .vnc/xstartup
#!/bin/sh

# Uncomment the following two lines for normal desktop:
# unset SESSION_MANAGER
# exec /etc/X11/xinit/xinitrc

[ -x /etc/vnc/xstartup ] && exec /etc/vnc/xstartup
[ -r $HOME/.Xresources ] && xrdb $HOME/.Xresources
xsetroot -solid grey
vncconfig -iconic &
#x-terminal-emulator -geometry 80x24+10+10 -ls -title "$VNCDESKTOP Desktop" &
#x-window-manager &

export XKL_XMODMAP_DISABLE=1
unset SESSION_MANAGER
unset DBUS_SESSION_BUS_ADDRESS
gnome-session&
gnome-panel&
gnome-settings-daemon&
gnome-terminal&
metacity&
nautilus&

```

再次启动vnc

```
#示例
#vncserver :实例号 -localhost 端口号
vncserver :50 -localhost 5950
```

---

本地计算机端：

打开Windows自带的远程桌面连接mstsc或者VNC Viewer

mstsc：

win + R， 输入mstsc， 输入ip地址 10.2.134.6， 选择vnc-any，输入ip 10.2.134.6 以及刚刚设置好的端口号和密码

VNC Viewer：

输入ip以及端口号 10.2.134.6:端口号，双击连接